# sample test case

# "The greatest victory is that which requires no battle" --> "battle no requires which that is victory greatest The"

# sample input
input_string = "The greatest victory is that which requires no battle"

# printing reversed word string
print(" ".join(input_string.split()[::-1]))
